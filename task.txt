=== DASHBOARD FIX ===
Issue: assigned/station counts counting rows instead of unique donors
Files changed:
  backend/src/controllers/ngoAdminController.js
    - assignedCount: from raw row count → unique donor_id (via donorInfo Map)
    - dataUsed/dataUnused: from row-based → donor-based (any connected status = used)
    - activeDonors/inactiveDonors: from row-based → donor-based
    - getStations: added donor_id + dedup by Set
  backend/src/models/froAssignmentModel.js
    - getStationDispositionStats: added donor_id selection, order by assigned_at desc, dedup by Set

Before: Assigned 32,501 (total 32,444) — +57 phantom duplicates
After:  Assigned 32,444 = Total

=== MY LEADS SEARCH FIX ===
Issue: searching a donor jumped to their position and lost your place
File changed:
  ucs crm/src/panels/fro/pages/MyDonors.jsx
    - Added returnToDonor state
    - handleSelectSearchResult: saves current position before navigating
    - handleSave: after save, returns to original donor (not advance from searched)
    - handleButtonClick: NEXT without disposition returns to original position
    - Counter shows "Searched donor — NEXT to return" hint when viewing from search

=== PERFORMANCE OPTIMIZATION ===
Issue: dashboard timeout "canceling statement due to statement timeout"
Files changed:
  backend/src/controllers/ngoAdminController.js
    - getDashboard: replaced getDonorProfilesByImportNgo (200+ parallel queries) with single SELECT + Set count
    - getDashboard: replaced findAssignmentsByNgo (SELECT * with joins, no limit) with 4-column select
    - getFroPerformance: replaced findAssignmentsByNgo with lightweight status/fro_worker_id select
  backend/src/models/froAssignmentModel.js
    - getStationDispositionStats: added 500K row safety limit
  backend/migrations/038_add_dashboard_indexes.sql (NEW — run this in Supabase SQL Editor)
    - 10 indexes on fro_assignments, donor_profiles, new_data, fro_donor_logs

Before: 200+ parallel queries + full scans with SELECT *, frequent timeouts
After:  Single queries with only needed columns, plus indexes for faster filtering

=== SUSPENSE SEARCH FIX ===
Issue: search input in Suspense tab couldn't type more than 1 character + shared state across rows
File changed:
  ucs crm/src/panels/ngo-admin/pages/Suspense.jsx
    - Fixed value={showDropdown === e.id ? searchQuery : ''} → value={searchQuery[e.id] || ''}
    - Changed searchQuery, searchResults, searching from single values to per-row maps { [entryId]: value }
    - Updated all references (onFocus, no-results check, click-outside handler)

Before: every keystroke erased the input (forced to '' by React), shared state polluted between rows
After:  input always shows typed text, each row has its own search state

=== DASHBOARD COUNTING CONSISTENCY FIX ===
Issue: collectedDonations row-based + total_workers using wrong field
Files changed:
  backend/src/controllers/ngoAdminController.js
    - collectedDonations: from array filter .length → Set by donor_id .size
  ucs crm/src/panels/ngo-admin/pages/Dashboard.jsx
    - total_workers: from Number(f.active) → Number(f.total)

Before: collectedDonations counted rows (same donor multiple times), Workforce total showed active count
After:  collectedDonations counts unique donors with donations, Workforce total shows total workers

=== FRO DASHBOARD COUNTING FIX ===
Issue: active donors always 0, data_used/unused row-based, stats.total row-based
Files changed:
  backend/src/controllers/froController.js
    - stats.total: from count rows → count distinct donor_ids (Set size)
    - assignmentsRes: from select('status') → select('status, donor_id') — fixes active=0 bug
    - dataUsed/dataUnused: from row-based → donor-based via donorInfo Map
    - activeDonors/inactiveDonors: from row-based → donor-based via donorInfo Map

Before: Active=0 (donor_id not selected), Data USed+Unused=assigned row count
After:  Active shows real count, Data Used/Unused = unique donors, stats.total = unique donors

=== CONNECTED STATUSES UPDATE ===
Add not_interested_now and callback as connected statuses
Files changed:
  backend/src/controllers/froController.js
    - connectedStatuses: added not_interested_now, callback
  backend/src/controllers/ngoAdminController.js (x2)
    - connectedStatuses: added not_interested_now, callback (both getDashboard + getFroPerformance)

These statuses now count as "used" for Data Used metric: contacted, donation_collected, lead_done, follow_up, scheduled, visit_donate, promise_to_pay, payment_pending, already_donated, language_barrier, transferred_senior, query_complaint, receipt_request, not_interested_now, callback

=== HR FORMS DECLARATION FIX ===
Issue: Declaration date empty, not showing worker joining date
Files changed:
  ucs crm/src/panels/hr/components/HRForms.jsx
    - handleSelectWorker: auto-fills declarationDate from data.created_at or data.declaration_date
    - handleSelectWorker: auto-fills place from data.declaration_place or 'Mumbai'

Before: Date field empty, Place empty
After:  Date auto-fills from worker joining date when employee selected

=== HR FORMS PRINT TEMPLATES FIX ===
Issue: declarationDate not passed to Templates 5/6, dates showing empty // in print
Files changed:
  ucs crm/src/panels/hr/components/forms/PrintForms.jsx
    - Passes declarationDate to Template5 and Template6
  ucs crm/src/panels/hr/components/forms/Template5.jsx
    - Date renders actual declarationDate instead of empty lines
    - Removed underline from Place value (Mumbai)
    - Removed redundant signing text line below Date/Place
    - Fixed signature box alignment (align-items: flex-start, 45% width)
  ucs crm/src/panels/hr/components/forms/Template6.jsx
    - Both Date fields render actual declarationDate instead of empty lines

=== HOW TO TEST ===
1. Dashboard: check Assigned = Total, station counts match per-station donor lists
2. My Leads: search a donor, process them, verify you return to your original position
3. Dashboard: load 3-4 times — should no longer timeout
4. Suspense: type in search field — should accept multiple characters and show results
5. FRO Dashboard: Active should show > 0, Data Used/Unused should sum to total assigned
6. After indexes: run migrations/038_add_dashboard_indexes.sql in Supabase SQL Editor

=== SATURDAY — JULY 18 ===

=== DASHBOARD & STATION FIXES (Skar200010) ===
Issue: station delete wiped all NGOs' stations, summary double-counted, performance score always 1.0 for single-worker NGOs
Files changed:
  backend/src/controllers/ngoAdminController.js
    - getDashboard: added late attendance tracking, fixed Used/Unused bar denominator to use assigned donors
    - getDashboard: added direct donation breakdown text on Verified card, Late/No Show attendance visibility in Workforce card
    - getFroPerformance: fixed performance score for single-worker NGOs (absolute check instead of relative)
    - removeStationByName: added ngo_id query param to scope delete to target NGO only
    - updateStationNgos: scope delete to specific ngo_id instead of wiping all rows for station
    - getStationStats: fixed summary double-counting — dedup by donor_id+status across shared NGOs
    - acknowledgeAlert/resolveDataRequest: fixed returning undefined data (crash fix)
    - getDonorFollowups: added NGO access checks (security)
    - CollectionDetailModal footer uses full data for leads count
  ucs crm/src/panels/ngo-admin/pages/Dashboard.jsx
    - Workforce card shows Late/No Show attendance
    - Verified card shows donation breakdown text
    - Reactivation filter consistency with active donors (accounts_status=verified)

=== FRO ONBOARDING WALKTHROUGH (Skar200010) ===
Issue: New FROs need guided tour of My Donors page features
Files changed:
  ucs crm/src/panels/fro/pages/MyDonors.jsx
    - Added walkthrough state + localStorage flag (fro_walkthrough_seen)
    - 7-step guided tour: Start Call → End Call → Take Break → Select Disposition → Log & Next → Search Donors → WhatsApp Chat
    - Step indicator with animated dot fill, Back/Skip/Next/Get Started navigation
    - Animated overlay with backdrop blur
  ucs crm/src/index.css
    - Walkthrough animations: card enter, icon pulse, dot fill, content enter

=== SCHEDULED AUTO-POP REWORK (Skar200010) ===
Issue: per-FRO auto-pop logic removed; replaced with global auto-pop at NGO level
Files changed:
  ucs crm/src/panels/fro/pages/Scheduled.jsx
    - Removed per-FRO polling/auto-pop logic (poppedIds, pollTick, auto-pop useEffect)
    - Removed green highlight on auto-popped rows
    - Simplified openModal/handlePopDone — always opens modal on click
  ucs crm/src/panels/fro/components/DispositionModal.jsx
    - Slightly larger modal for better UX

=== STATION SEED NGO SCOPING + ORPHAN CLEANUP (Skar200010) ===
Issue: seedStations didn't accept NGO scope, no way to clean orphaned seed rows
Files changed:
  backend/src/controllers/ngoAdminController.js
    - seedStations: accept optional ngo_id to seed stations for specific NGO only
    - updateStationNgos: use .is('ngo_id', null) instead of .eq for null NGO delete
    - Added POST /stations/cleanup endpoint to delete orphaned seed rows (fro_worker_id IS NULL)
  backend/src/routes/ngoAdminRoutes.js
    - Added cleanup route
  ucs crm/src/panels/ngo-admin/pages/StationManagement.jsx
    - Pass selectedNgoId to seed endpoint
    - Added Cleanup Orphaned button

=== VIEW ALL CRM TIMELINE (digitalmrk990-cpu) ===
Issue: CRM timeline in My Donors only showed recent logs, no way to see full history
Files changed:
  ucs crm/src/panels/fro/pages/MyDonors.jsx
    - Added View All button to donor timeline to see full log history

=== HR — WORKERS COMPONENT OVERHAUL (Shonferns004) ===
Issue: Workers component needed cleanup, HRForms tweaks, declaration date fixes
Files changed:
  backend/src/controllers/workerController.js
    - Cleanup: removed unused code, streamlined worker queries
  backend/src/models/onboardingModel.js
    - Removed unused model code
  backend/src/routes/workerRoutes.js
    - Removed unused routes
  ucs crm/src/panels/hr/components/Workers.jsx
    - Overhauled component structure
    - Fixed worker listing and management
  ucs crm/src/panels/hr/components/HRForms.jsx
    - Auto-fills declarationDate from worker joining date
    - Auto-fills place from declaration_place or 'Mumbai'
  ucs crm/src/panels/hr/components/forms/PrintForms.jsx
    - Passes declarationDate to Template5 and Template6
  ucs crm/src/panels/hr/components/forms/Template5.jsx
    - Renders actual declarationDate instead of empty lines
    - Removed underline from Place value (Mumbai)
    - Removed redundant signing text line below Date/Place
    - Fixed signature box alignment (align-items: flex-start, 45% width)
  ucs crm/src/panels/hr/components/forms/Template6.jsx
    - Both Date fields render actual declarationDate instead of empty lines

=== WHATSAPP CRM — INBOX REDESIGN + TEMPLATE EDITOR (Shonferns004) ===
Issue: WhatsApp inbox needed redesign with message grouping, template sending, and conversation agent assignment
Files changed:
  whatsapp-crm/src/panels/whatsapp-crm/pages/InboxPage.tsx
    - Redesigned avatar system: letter-based instead of SVG silhouette, color hashing
    - Added DateSeparator component (Today/Yesterday/full date)
    - Added MessageBubble component with group start/end/alone modes
    - Added showAvatar toggle for grouped consecutive messages
    - Removed ConversationLabels, LabelFilter, old Search/Plus icons
    - Smaller message status icons with proper coloring (failed=red)
    - Integrated TemplateBar for sending WhatsApp templates
  whatsapp-crm/src/panels/whatsapp-crm/lib/whatsapp.ts
    - Added extractPlaceholders + extractParamKeys for template parameter parsing
    - Added fetchMetaTemplateComponents to fetch template structure from Meta API
    - Added buildTemplateComponents to build parameter arrays for template send
    - Auto-assigns conversation to current agent on first message send
  whatsapp-crm/src/panels/whatsapp-crm/pages/TemplateEditorPage.tsx
    - Template editor updates
  whatsapp-crm/src/panels/whatsapp-crm/components/chat/MessageComposer.tsx
    - Composer improvements
  whatsapp-crm/src/panels/whatsapp-crm/components/layout/Header.tsx
    - Header layout updates

=== WHATSAPP CRM — CONVERSATION AGENT MIGRATION (Shonferns004) ===
Issue: conversations need assigned_agent_id column for agent routing
Files changed:
  backend/src/migrations/033_conversation_assigned_agent.sql
    - Added assigned_agent_id column to conversations table

=== TASK SHEETS + HR ONBOARDING FORM (Shonferns004) ===
Files added:
  task_sheets.xlsx — Task tracking spreadsheet
  hr form/onboarding.html — HR onboarding form template
  docs/generate_tasks.js — Task generation script
  docs/package.json / docs/package-lock.json — Docs dependencies

=== FIX — WORKERS COMPONENT MINOR FIX (Shonferns004) ===
Files changed:
  ucs crm/src/panels/hr/components/Workers.jsx
    - Minor fix (3 lines changed, 4 removed)

=== WORKER CONTROLLER + EMPLOYEE DETAIL FIXES (Shonferns004) ===
Files changed:
  backend/src/controllers/workerController.js
    - Worker controller fixes
  ucs crm/src/panels/hr/components/EmployeeDetail.jsx
    - Employee detail component updates
  ucs crm/src/panels/hr/components/Workers.jsx
    - Workers component updates

(End of file)
